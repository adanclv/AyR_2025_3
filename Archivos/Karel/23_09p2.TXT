class program {
// que coma 3
// hacer la letra

    void turnRight() {
        iterate(3) { turnleft(); }
    }

    void go() {
        while(frontIsClear) {
            while(nextToABeeper) {
                pickbeeper();
                if(nextToABeeper) {
                    putbeeper();
                }
            }
            move();
        }
    }

    void retorno(a) {
        if(nextToBeeper) {
            pickbeeper;
        }
        if(iszero(a)) {
            turnRight();
            move();
            turnRight();
        } else {
            turnleft();
            if(frontIsClear) {
                move();
            }
            turnleft();
        }

    }

    program() {
        iterate(5) {
            go();
            retorno(0);
            go();
            retorno(1);
        }

        turnoff();
    }
}
